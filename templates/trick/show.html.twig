{% extends 'base.html.twig' %}

{% block title %}La page des figures{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>La figure ! ✅</h1>
        <a href="{{ path('trick_edit', {'id': trick.id}) }}">Modifier la figure</a>
        <h2>{{ trick.title }}</h2>
        <p>{{ trick.description }}</p>
        <span class="date">{{ trick.createdAt|date('d/m/Y') }}</span>
        <br><br>
        <a href="{{ path('trick_remove', {'id': trick.id}) }}">Supprimer la figure</a>
    </div>
{% endblock %}
